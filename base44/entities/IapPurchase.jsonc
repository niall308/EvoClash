{
  "name": "IapPurchase",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string"
    },
    "platform": {
      "type": "string",
      "enum": [
        "apple",
        "stripe",
        "migrated_from_stripe"
      ],
      "description": "Which billing platform produced this purchase. Apple = a validated StoreKit receipt; 'migrated_from_stripe' = a record copied from the legacy Stripe CoinTransaction ledger."
    },
    "productId": {
      "type": "string",
      "description": "Apple product id (com.primallegends.coin_pack_*). For migrated Stripe records this holds the internal packId."
    },
    "packId": {
      "type": "string",
      "default": "",
      "description": "Internal pack id shared with COIN_PACKS, so a single catalog maps Stripe + Apple products."
    },
    "transactionId": {
      "type": "string",
      "default": "",
      "description": "Idempotency anchor. Apple transaction_id, or 'stripe:<sessionId>' for migrated records. A replayed validation with the same transaction is a no-op."
    },
    "purchaseToken": {
      "type": "string",
      "default": "",
      "description": "Raw Apple receipt base64 retained for restore / server-side re-validation."
    },
    "purchaseDate": {
      "type": "string",
      "default": ""
    },
    "expiryDate": {
      "type": "string",
      "default": "",
      "description": "Subscriptions only. Empty for consumable coin packs."
    },
    "coins": {
      "type": "number",
      "default": 0,
      "description": "LC granted for this purchase. 0 for migrated Stripe records (coins were already granted by the Stripe webhook)."
    },
    "priceUsd": {
      "type": "number",
      "default": 0
    },
    "consumed": {
      "type": "boolean",
      "default": false
    },
    "acknowledged": {
      "type": "boolean",
      "default": false
    },
    "migratedFrom": {
      "type": "string",
      "default": "",
      "description": "Set to 'stripe' for records copied from CoinTransaction during the one-off migration."
    },
    "rawResponse": {
      "type": "object",
      "default": {},
      "description": "Raw validated response from Apple, or the source CoinTransaction snapshot for migrated records."
    }
  },
  "required": [
    "userId",
    "platform",
    "productId"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "data.userId": "{{user.id}}"
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}