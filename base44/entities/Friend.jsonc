{
  "name": "Friend",
  "type": "object",
  "properties": {
    "friendUserId": {
      "type": "string"
    },
    "friendName": {
      "type": "string"
    }
  },
  "required": [
    "friendUserId",
    "friendName"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}