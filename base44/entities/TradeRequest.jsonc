{
  "name": "TradeRequest",
  "type": "object",
  "properties": {
    "toUserId": {
      "type": "string"
    },
    "toUserName": {
      "type": "string"
    },
    "fromCardId": {
      "type": "string"
    },
    "fromCardName": {
      "type": "string"
    },
    "fromCardType": {
      "type": "string"
    },
    "fromCardTier": {
      "type": "number"
    },
    "fromCardAttack": {
      "type": "number"
    },
    "fromCardDefense": {
      "type": "number"
    },
    "fromCardBonusDamage": {
      "type": "number",
      "default": 0
    },
    "fromCardIsHybrid": {
      "type": "boolean",
      "default": false
    },
    "fromCardImageUrl": {
      "type": "string"
    },
    "toCardId": {
      "type": "string"
    },
    "toCardName": {
      "type": "string"
    },
    "toCardType": {
      "type": "string"
    },
    "toCardTier": {
      "type": "number"
    },
    "toCardAttack": {
      "type": "number"
    },
    "toCardDefense": {
      "type": "number"
    },
    "toCardBonusDamage": {
      "type": "number",
      "default": 0
    },
    "toCardIsHybrid": {
      "type": "boolean",
      "default": false
    },
    "toCardImageUrl": {
      "type": "string"
    },
    "coins": {
      "type": "number",
      "default": 0,
      "description": "LC coins the proposer includes in the deal, paid to the recipient on acceptance. Can be changed via counteroffers."
    },
    "lastOfferBy": {
      "type": "string",
      "enum": [
        "proposer",
        "recipient"
      ],
      "default": "proposer",
      "description": "Who set the current coins value / whose offer is on the table awaiting a response."
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "completed",
        "declined",
        "cancelled",
        "expired"
      ],
      "default": "pending"
    },
    "proposerClaimed": {
      "type": "boolean",
      "default": false
    },
    "recipientClaimed": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "toUserId",
    "toUserName",
    "fromCardId",
    "fromCardName",
    "toCardId",
    "toCardName"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.toUserId": "{{user.id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.toUserId": "{{user.id}}"
        }
      ]
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}